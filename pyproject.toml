[project]
name = "pvpp-runtime-prototype"
version = "0.1.0"
description = "Exploratory executable PV-PP runtime architecture prototype"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
